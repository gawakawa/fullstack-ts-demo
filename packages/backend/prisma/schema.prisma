generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://user:password@mysql:3306/appdb"
}

model User {
  id   Int    @id @default(autoincrement())
  name String
}
